#include "main.h"

/**
 * _isupper - function that checks uppercase character
 * Return: 1 if c is uppercase 0 otherwise
 */

int _isupper(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
{
return (0);
}
}
